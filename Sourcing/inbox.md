# Inbox

Raw finds, newest on top. Unreviewed — nothing here is trusted until
triaged (see `CLAUDE.md`'s triage rule) and moved into a curated file.

Format per entry:

```
## YYYY-MM-DD — <one-line title>
Source: <url>
Type: interview-account | job-description | resource
Summary: <2-3 sentences>
```

(No entries yet — first scheduled run will populate this.)
